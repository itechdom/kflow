service: knowledgeflow-api

provider:
  name: aws
  runtime: nodejs14.x
  stage: dev
  region: us-east-1

functions:
  app:
    handler: handler.run
    events:
      - http:
          path: /
          method: get